/**
 * PWA desativada em vite.config (vite-plugin-pwa disable: true).
 * Quando reativar o plugin, restaure o registo com virtual:pwa-register/react.
 */
export function PWAUpdatePrompt() {
  return null;
}
